# 2026-08-22

## Echo 原型：黑胶唱片对标网易云 redesign
- 文件：`docs/prototype/echo-desktop-player.html`
- 唱片盘面：中心改为满版"封面"圆（inset 15.5% + 5px 黑缝），去掉 record-label 文字；黑胶纹路更细密，外圈 rim 改为冷灰渐变，盘面高光调暗。
- 唱臂：重绘为网易云彩白色 S 形弯臂（SVG path `M204 26C212 66 220 104 238 138C252 164 268 174 284 178` + 针头组 rotate(30)），转轴在唱片正上方约 1.5R 处。
- 播放/暂停语义修正：暂停 = `rotate(-25deg)` 唱臂抬到唱片右上方外侧；播放 = `rotate(0)` 落在盘面 0.8R 处（与之前方向相反，注意别回退）。
- 播放页底色：暖棕 `color-mix(fg, warn 28%)` → 冷青灰 `#33373f`（popover + player-mode playerbar 两处）。
- 验证方法：无 agent-browser 环境下，用 sed 生成强制打开 popover 的临时副本 + 系统 Chrome `--headless --no-sandbox --screenshot` 截图比对（沙箱内 Chrome 必须加 --no-sandbox）。
- 后续微调（xian 要求）：固定点 pivot 缩小 10%（r 13.5→12.2，hub 4.5→4）；主臂 path 起点改为圆心 `M196 14`，并把 pivot/hub 圆移到主臂 path **之后**绘制，让实心白圆盖住臂根部 → 臂从固定点中心出发、不穿过固定点。
- xian 反馈"还是穿过"：根因是我之前在 pivot 上方加的平衡锤 stub（`M196 2V-8`）从固定点顶部探出一截白柱，视觉上像穿模 → 已删除。验证技巧：给 pivot 注入红描边标记 + 冻结封面旋转动画，headless Chrome 截图后用 PIL 定位红圈坐标，再数其上方窗口的白色像素（结果 0=0）。注意直接 diff 前后截图会被 record-spin 封面动画污染。
- 生成独立复刻文件：`docs/prototype/netease-vinyl-replica.html`。用 OpenCV HoughCircles 从截图 `clipboard_2026-08-22_01-02.png` 定位网易云唱片外圈 rim（315,407,r=235），按 0.66 比例裁出专辑封面并 base64 嵌入；单独实现黑胶、唱臂、播放/暂停切换动画，作为后续往 Echo 原型迁移的参考。
- 复刻 v1 被 xian 吐槽后重看参考图，发现臂太短、封面太小、rim 太粗等硬伤 → v2 修正：封面半径按 rim 0.82 重新裁取（更大），黑胶外圈 rim 改薄亮金属灰，唱臂改为"直 → 高转折圆角 → 直"结构，needle 移到盘面 2:30 位置；viewBox 扩到 330×330 避免 pause 态唱头被裁切。最终两态截图和网易云参考已非常接近。

## Echo 原型治理：确立 v2 为唯一权威原型
- 决策（xian 确认）：`docs/prototype/echo-desktop-player.html` = 唯一原型；`PRODUCT.md` = PRD（保留）。其余非 PRD 文档迁入 `待删除/`（保留相对路径，可回退）。
- 迁移清单：根目录旧 `echo-desktop-player.html` + artifact.json；docs 下 3 份旧/探索原型（`echo-desktop-player.html` / `echo-color-options.html` / `netease-vinyl-replica.html`）；设计规范 MD（`DESIGN.md` / `interface-terminology.md` / `brand-spec.md`）；SVG 素材（`echo-icon.svg` / `echo-tonearm-study.svg`）及各自 artifact.json。
- 关键事实：v2 完全自包含（品牌图标为内联 SVG，第 429 行八分音符 path），不依赖被迁走的 SVG；仅把 `echo-desktop-player.html.artifact.json` 归位以保持预览绑定。
- 注：PRODUCT.md 当时被误置 `待删除/`，其第 39/44 行指向的原型路径因 v2 改名而**正确**（原型仍在 `docs/prototype/`）；第 45/46 行指向的 `tonearm-study.svg` / `netease-vinyl-replica.html` 仍在 `待删除/` 归档（引用未变）。

## 18:41 只读复核：docs/ PRD + 原型（修正上一条的过时状态）
- **当前真实状态（与上一条"PRD 保留根目录"相反）**：`PRODUCT.md`（带 `product-schema` 的权威 PRD）现位于 `待删除/PRODUCT.md`，不在 docs/ 也不在根。其第 39 行指向 `docs/prototype/echo-desktop-player.html` —— 因上次 v2 改名，路径巧合正确；但文件本体被误置待删除。
- **重复文档**：`docs/DESIGN.md` 与 `docs/interface-terminology.md` 在 `待删除/docs/` 下各有一份**完全一致**的副本（diff 为 SAME）。docs/ 为活副本，待删除/ 为冗余副本。
- **品牌基准缺失**：`brand-spec.md`（珊瑚玫红 OKLch 基准）现在只在 `待删除/docs/prototype/`，docs/ 内已无 → 原型配色在 docs/ 内无可对照规范。
- 原型复查结论：与 `interface-terminology.md` 逐条吻合（侧边栏四区/新建歌单/主题/真静音/进度条横跨交界/沉浸式黑胶/歌曲操作菜单/Toast 全部命中）；仍残 Ant 模板注释（第 9–12 行）+ 字体名 "Ant Sans"（第 32–33 行）；第 774 行排除列表漏 `previous/next-track`，导致上一首/下一首点击误持 `.active`（瞬时动作不应常亮）；顶栏计数 1,343 首但实际渲染 100 行。
- 本次为只读复核，未改动任何文件。建议后续：删待删除/ 中 DESIGN/术语冗余副本；brand-spec.md 放回 docs/；清 Ant 残留注释 + 修上一首/下一首 `.active`。

## 19:44 执行：PRODUCT.md 移入 docs/
- 将 `待删除/PRODUCT.md` 移到 `docs/PRODUCT.md`（不再误置待删除）。
- 校验：原型路径引用（第 39/44 行 `docs/prototype/echo-desktop-player.html`）仍有效（原型位置未变）；第 45/46 行指向的 `tonearm-study.svg` / `netease-vinyl-replica.html` 仍在 `待删除/` 归档（引用维持原样）。
- 现状收口：docs/ 内现有 `PRODUCT.md`(PRD) + `DESIGN.md` + `interface-terminology.md` + `prototype/`（权威原型 + 三主题图标）。待删除/ 中仍含 DESIGN/术语冗余副本、brand-spec.md、tonearm/netease 探索稿。

## 19:47 执行：待删除/ 收口（删冗余副本+旧原型，brand-spec 移回 docs/）
- 依据上一轮只读分析执行。删除（精确列名，未碰 keep-list）：
  - `待删除/docs/DESIGN.md`、`待删除/docs/interface-terminology.md`（与 docs/ SAME 副本）
  - `待删除/docs/prototype/echo-icon.svg`、`待删除/docs/prototype/echo-icon_副本.svg`（与活动 echo-icon.svg SAME 副本）
  - `待删除/echo-desktop-player.html`(+artifact)、`待删除/docs/prototype/echo-desktop-player.html`(+artifact)（旧 Ant 红原型，已被 v2 取代）
  - `待删除/docs/prototype/echo-color-options.html`(+artifact)（探索稿，零引用）
  - 两处 `.DS_Store`
- 保留未动：`待删除/docs/prototype/echo-tonearm-study.svg`、`netease-vinyl-replica.html`（PRODUCT.md 第45/46行引用，删即断链）、全部图片（设计参考素材，本不该进待删除/）。
- 品牌基准：`待删除/docs/prototype/brand-spec.md` → `docs/prototype/brand-spec.md`（原出处，原型配色现可在 docs/ 内对照）。
- 现状：docs/ 含 PRODUCT.md + DESIGN.md + interface-terminology.md + prototype/(权威原型+三主题图标+brand-spec.md)。待删除/ 仅剩 PRD 引用两文件 + 14 张图片。
- 未决项（用户未授权）：① PRODUCT.md 第45/46行仍指向归档路径，可改/标注；② 原型第9–12行 Ant 残留注释 + 第774行上一首/下一首误持 `.active`；③ 待删除/ 图片去向（移回根 or 单独归档）。

## 19:49 执行：收口项 1 & 2（项 3 用户叫停，不动）
- 项 1（PRD 断链修复）：`docs/PRODUCT.md` 第45/46行路径改为 `待删除/docs/prototype/echo-tonearm-study.svg` / `netease-vinyl-replica.html`，并标注「（已归档）」。引用现可解析到真实位置。
- 项 2a（Ant 注释残留）：原型第9–12行 `design-systems/ant/tokens.css … sharp red intent` 注释替换为 Echo 品牌说明；第32–33行 `--font-display/--font-body` 的 `"Ant Sans"` 一并清除，改为 `"Alibaba PuHuiTi", Inter, Arial, sans-serif`。
- 项 2b（上一首/下一首误持 .active）：原第774行 `:not(#id)` 排除对 previous/next-track 无效（这两按钮**无 id，只有 data-od-id**），改为补 `:not([data-od-id="previous-track"]):not([data-od-id="next-track"])`。点击不再常亮。
- 校验：grep 确认 Ant 残留（design-systems/ant / Ant Sans / Structured token bindings）已全清；第774行排除列表含两个 data-od-id。
- 剩余未决：仅项 3（待删除/ 14 张图片去向），用户暂不动。
