# Echo 0.1.0 scenario executable manifest (sync-foundation, generated for the 0005 shape-ready capability)
# ----------------------------------------------------------------------
scenario_id: SYN-R03-S01
requirement: 墓碑
scenario: 删除产生墓碑
layer: automated
command: "cargo test -p echo-core --all-features --lib infrastructure::sqlite::tests::sync_foundation"

# The sqlite suite asserts the transaction-atomic facts directly: outbox
# rows with full relative-path-only snapshots, monotone object revisions,
# tombstones on Echo delete, and the offline boundary (nothing pushes).
fixtures: []
expected_result: command exits 0 (behavior proven by the targeted suite)
automated_filter: the command above
