# Echo 0.1.0 scenario executable manifest (generated by gen-scenario-manifests.mjs)
# ----------------------------------------------------------------------
scenario_id: LL-R05-S02
requirement: SQLite 检索与曲库排序
scenario: 重启后数据可用
layer: automated
command: "cargo test -p echo-core --all-features infrastructure::sqlite::tests::keyset_pages_are_deterministic"

# Normal path + critical failure path are proven by the automated test the
# command runs; this manifest records what that test covers.
fixtures: [tone-short.mp3, tone-short.flac, tone-short.m4a, cover-256.png]
expected_result: command exits 0 (behavior proven by the targeted suite)
automated_filter: the command above
