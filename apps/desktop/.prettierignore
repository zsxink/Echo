# Tauri derives these schemas from tauri.conf.json during Cargo builds.
# They must be regenerated, not hand-formatted.
src-tauri/gen/

# Generated by `cargo run -p echo-desktop --bin echo-generate-ipc` and
# drift-locked to the Rust generator (task 7.2). Must stay byte-for-byte in
# sync with the generator, so never prettier-format it.
src/ipc/ipc-types.generated.ts

# Vite production output is generated by `pnpm build`.
dist/

# Extracted verbatim from docs/prototype/echo-desktop-player.html by
# `node scripts/extract-prototype-css.mjs` (task: 界面严格对齐原型). The app must
# ship the prototype's own declarations byte-for-byte, so never prettier-format
# them — re-run the extraction after editing the prototype instead.
# `src/styles/app-extras.css` is NOT listed: it is hand-written app code.
src/styles/tokens.css
src/styles/shell.css
src/styles/library.css
src/styles/player.css
src/styles/responsive.css

# Vendored third-party libmpv supply-chain assets (task 9.8 platform Gate).
# manifest.json carries its own fixed 1-space-indent format that the Gate
# checks (scripts/verify/manifest.json rule); NOTICE/BUILD_INFO are provenance
# records kept verbatim. Never prettier-format them.
src-tauri/vendor/

# Vitest writes these temp config mirrors next to vitest.config.ts.
vitest.config.ts.timestamp-*.mjs
