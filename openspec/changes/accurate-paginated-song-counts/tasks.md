## 1. Core 分页总数

- [x] 1.1 为分页查询保留筛选条件匹配的总数，确保 SQL 计数不包含游标条件且计数、页内容和游标版本来自同一快照；通过 Core 分页查询测试覆盖首页、后续页和搜索过滤。
- [x] 1.2 让内存目录分页和 IPC `PagedSongs` 一致传递总数；运行相关 Rust 测试并重新生成 TypeScript IPC 类型，确认生成结果无差异。

## 2. 桌面计数显示

- [x] 2.1 将资料库列表标题绑定查询总数，并为最近添加保持定义内计数；在 UI 测试中覆盖超过单页的全量、收藏及搜索结果和后续页加载。
- [x] 2.2 复核歌单、最近添加、歌手和专辑列表计数来源；运行受影响的桌面测试、TypeScript 类型检查与构建检查。

## 3. 最终复核

- [x] 3.1 执行 OpenSpec 校验、格式检查和改动复核，确认需求场景、IPC 合约与实现一致。
