## 1. 资料库领域与持久化

- [x] 1.1 为歌手和专辑定义稳定聚合身份、目录 DTO、封面来源与详情歌曲查询输入，并通过 Rust 单元测试覆盖 Unicode/大小写归一化、缺省元数据和不同歌手同名专辑合并。
- [x] 1.2 新增顺序 SQLite migration 及 Repository 实现以持久化歌手封面覆盖记录，并通过迁移升级、重建资料库投影和写入失败回滚测试验证既有歌曲与覆盖层不受影响。
- [x] 1.3 实现只读取当前根 available 歌曲的歌手/专辑聚合查询与详情歌曲查询，并通过单元测试验证歌曲计数、按最近加入时间选择歌手自动封面、确定性排序、空封面占位和跨根隔离。
- [x] 1.4 实现歌手封面设置、替换和清除用例，复用受控封面存储并通过单元测试验证“自定义覆盖 → 最新专辑 → 占位”优先级、持久化和失败保留旧值。
- [x] 1.5 运行 `cargo fmt --all --check`、`cargo clippy --workspace --all-targets --all-features -- -D warnings` 与覆盖上述用例的 Rust 测试，修复检查发现的问题。

## 2. 桌面边界与刷新

- [x] 2.1 为聚合目录、详情歌曲查询和歌手封面写入新增类型化 Tauri command/DTO，并通过 command 序列化或集成测试验证输入校验、错误结构和不泄漏本机绝对路径。
- [x] 2.2 将扫描、导入、歌曲封面/元数据更新、歌手封面写入和根切换接入目录与详情查询的失效路径，并通过集成测试验证旧根或过期请求不能提交结果。
- [x] 2.3 运行 IPC 生成/漂移检查及相关 Rust 集成测试，确认新增边界不改变既有歌曲列表 command 的返回格式。

## 3. 桌面资料库体验

- [x] 3.1 扩展资料库导航和状态模型，支持歌手目录、专辑目录与稳定聚合身份的详情页，并通过 React 测试验证视图切换、返回入口和根切换时旧结果被忽略。
- [x] 3.2 实现可复用的歌手/专辑目录条目，展示封面、名称、歌曲数量及同名专辑的歌手信息，并通过组件测试验证继承封面和占位封面状态。
- [x] 3.3 在详情页复用歌曲播放、收藏、单曲菜单和既有全选/多选能力，并通过组件测试验证歌手/专辑目录不显示多选、而其详情显示当前歌曲的全选入口。
- [x] 3.4 接入目录和详情搜索、空状态、加载/不可用反馈及刷新保留策略，并通过 React 测试验证搜索范围、清空搜索和扫描后更新。
- [x] 3.5 实现歌手封面编辑、替换、清除与失败反馈交互，并通过组件测试验证仅在确认写入后更新界面、失败时保留旧封面。
- [x] 3.6 更新 `docs/prototype/echo-desktop-player.html` 与 `docs/interface-terminology.md` 以反映已实现的歌手/专辑导航、封面、目录无多选与详情全选交互，并人工核对桌面、窄屏与键盘返回行为。

## 4. 集成验证与交付

- [x] 4.1 运行 `pnpm --filter @echo/desktop lint`、`pnpm --filter @echo/desktop typecheck`、`pnpm --filter @echo/desktop test` 和 `pnpm --filter @echo/desktop build`，修复与本变更相关的失败。
- [x] 4.2 使用包含缺失元数据、同名专辑、多封面歌手和根目录切换的临时资料库执行端到端冒烟，确认目录、封面回退、歌手封面持久化、详情播放和无多选均符合规格。
- [x] 4.3 运行 `openspec validate add-artist-album-views --strict`、`git diff --check`，并在交付前记录所有验证结果。
